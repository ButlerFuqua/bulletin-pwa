
export type TestimonyResponse = {
    id: string
    author_id: string
    body: string
    author_username: string
    author_avatar_url: string
    org_slug: string
    org_Location: string
}

export type TestimonyDTO = {
    id: string
    authorId: string
    body: string
    authorUsername: string
    authorAvatarUrl: string
    orgSlug: string
    orgLocation: string
}